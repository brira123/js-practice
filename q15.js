let isRaining = false;
let isSnowing = false;
let windSpeed = 14;
let isCloudy = false;

if (isRaining === false && isSnowing === false && windSpeed < 15 && isCloudy === false) {
    console.log("A hypothetical rocket can launch");
} else {
    console.log("A hypothetical rocket cannot be launch");
}

